{
  "name": "unocalc",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:assets": "pwa-assets-generator",
    "build:pkg": "cd .. && wasm-pack build --out-name index && rm pkg/.gitignore && cd ./app && npm install",
    "preview": "vite preview",
    "prepare": "npm run build:assets && npm run build:pkg"
  },
  "devDependencies": {
    "@vite-pwa/assets-generator": "^0.2.4",
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.40",
    "tailwindcss": "^3.4.7",
    "typescript": "^5.5.4",
    "vite": "^5.3.5",
    "vite-plugin-pwa": "^0.20.1",
    "vite-plugin-top-level-await": "^1.4.2",
    "vite-plugin-wasm": "^3.3.0"
  },
  "dependencies": {
    "@vercel/analytics": "^1.3.1",
    "unocalc": "file:../pkg"
  }
}
